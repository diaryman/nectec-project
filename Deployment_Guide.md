# 🐳 คู่มือการติดตั้งบน Linux Server (Docker)

คู่มือนี้จะแนะนำวิธีการนำระบบ **Smart Court AI** ขึ้นรันบน Server (เช่น AWS EC2, Ubuntu, CentOS) ด้วย Docker และ Docker Compose

---

## 🏗️ 1. เตรียม Server

### 1.1 ติดตั้ง Docker
หาก Server ยังไม่มี Docker ให้รันคำสั่งต่อไปนี้ (สำหรับ Ubuntu):

```bash
# Update package list
sudo apt-get update

# Install Docker
sudo apt-get install -y docker.io

# Start & Enable Docker
sudo systemctl start docker
sudo systemctl enable docker

# Add user to docker group (จะได้ไม่ต้องพิมพ์ sudo บ่อยๆ)
sudo usermod -aG docker $USER
```

*หลังจากรันคำสั่งสุดท้าย ให้ Logout แล้ว Login ใหม่หนึ่งครั้งเพื่อให้สิทธิ์มีผล*

### 1.2 ติดตั้ง Docker Compose
```bash
sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

---

## 🚀 2. ติดตั้งและรันโปรแกรม

### 2.1 อัปโหลดไฟล์ขึ้น Server (หรือ Git Clone)
ตรวจสอบให้แน่ใจว่าบน Server มีไฟล์สำคัญครบถ้วน:
*   `Dockerfile`
*   `docker-compose.yml`
*   `requirements.txt`
*   `main.py`
*   `src/` (folder)
*   `.streamlit/secrets.toml` (ต้องสร้างหรือ copy ขึ้นไป)
*   `credentials.json` (ต้อง copy ขึ้นไปเพื่อใช้ Google Sheet)

### 2.2 สั่งรันด้วย Docker Compose
เข้าไปที่ folder ของโปรเจกต์ แล้วรันคำสั่ง:

```bash
# สร้าง Image และรัน Container (โหมด Background)
docker-compose up -d --build
```

### 2.3 ตรวจสอบสถานะ
```bash
# ดูรายการ Container ที่รันอยู่
docker-compose ps

# ดู Logs การทำงาน
docker-compose logs -f
```

---

## 🌐 3. การเข้าใช้งาน
เมื่อรันสำเร็จ ระบบจะเปิด Port **8502** ไว้ (ตามที่ตั้งใน `docker-compose.yml`)

เข้าใช้งานผ่าน Browser:
`http://<IP-Address-Of-Server>:8502`

---

## 🛠️ การแก้ไขและอัปเดต
หากมีการแก้โค้ด ให้รันคำสั่งใหม่เพื่อให้ระบบสร้าง Image ใหม่:
```bash
docker-compose down
docker-compose up -d --build
```

---

## ⚠️ หมายเหตุเรื่อง Google Quota
หากรันบน Cloud (AWS/GCP/Azure) แล้วเจอ Error `429 Limit: 0` สาเหตุเพราะ IP ของ Cloud Server ถูก Google มองว่าเป็น Bot
**วิธีแก้:**
1.  **ต้องเปิด Billing (ผูกบัตร)** ใน Google AI Studio
2.  หรือใช้รุ่น **Gemini 2.0 Flash** ซึ่งบางครั้งอาจจะอนุโลมมากกว่ารุ่น Pro
